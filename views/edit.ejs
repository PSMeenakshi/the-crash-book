<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Edit Book</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <div class="container">
    <h1>Edit Book</h1>

    <form action="/books/<%= book.id %>" method="POST">
      <label>Title (required)
        <input name="title" value="<%= book.title %>" required>
      </label>

      <label>Author
        <input name="author" value="<%= book.author %>">
      </label>

      <label>ISBN (for cover image)
        <input name="isbn" value="<%= book.isbn %>">
      </label>

      <label>Rating (1–5)
        <input name="rating" type="number" min="1" max="5" value="<%= book.rating %>">
      </label>

      <label>Date Read
        <input name="date_read" type="date" value="<%= book.date_read ? book.date_read.toISOString().split('T')[0] : '' %>">
      </label>

      <label>Notes
        <textarea name="notes" rows="4"><%= book.notes %></textarea>
      </label>

      <div class="button-group">
  <button type="submit">Add</button>
  <a href="/" class="button button-secondary">Cancel</a>
</div>
    </form>
  </div>
</body>
</html>